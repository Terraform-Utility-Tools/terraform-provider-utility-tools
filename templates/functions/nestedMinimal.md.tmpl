---
page_title: "{{.Name}} Function - {{.RenderedProviderName}}"
description: |-
  {{.Summary | trimspace | plainmarkdown}}
---

# `{{.Name}}` Function

{{.Summary}}

{{.Description}}

{{.FunctionSignatureMarkdown}}

{{.FunctionArgumentsMarkdown}}
{{- if .FunctionVariadicArgumentMarkdown}}
{{.FunctionVariadicArgumentMarkdown}}
{{- end}}

## Example Usage

```console
> provider::util::nestedMinimal({ dev = { api = { port = 8080 }, cache = {} }, prod = { api = { port = 8080 }, db = null } })
tomap({
  dev  = tomap({ api = tomap({ port = 8080 }) })
  prod = tomap({ api = tomap({ port = 8080 }) })
})
```

~> **Note:** `nestedMinimal` removes both null values and empty collections at any depth. Use `nestedCompact` if you only want to strip nulls.

### HCL Example

{{codefile "terraform" "examples/functions/nested_minimal/functionNestedMinimal.tf"}}
